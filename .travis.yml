language: C

install:
  # Prerequisites
  -  "sudo apt-get install texinfo help2man cvs rcs autoconf autoconf-doc autoconf-archive automake-1.14"

# try to build:
script: ./configure --enable-staticlib && make && make check && make distcheck

compiler:
  - clang
  - gcc
